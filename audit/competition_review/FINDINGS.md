# FINDINGS.md — 问题清单（阶段1–3定稿版，后续阶段继续追加）

状态: OPEN / CONFIRMED / RESOLVED(已核实结案) / SUPERSEDED。严重度: P0 Blocker / P1 Critical / P2 Major / P3 Minor / Note。
置信度: 已证实=有A/B级直接证据；高概率=证据强但未穷尽；待核实；无法验证。

---

## F-01 · P1 · 生产推理环境与当前机器完全脱节 — CONFIRMED（已证实）

- 受影响要求: R1/R2/R7/R9；内部维度: 可复现性、部署
- 事实: `predict_best_fusion.py:20` 硬编码 `ROOT=C:\AI\fire_detection`，`:64` 缓存目录 `C:\AI\hf_cache\hub`；`scripts/predict_best_fusion_crop.ps1:19` 指定 `C:\ANACONDA2\envs\siglip_learn\python.exe` 并设置全部 C:\AI 环境变量(:11-16)。本机该三处均不存在[E009]；当前 venv 缺 ultralytics/transformers/timm/gradio[E004]。其余 ps1 亦引用 torch_gpu 环境。
- 影响: 在任何新环境(含本机)按默认入口无法运行→端到端不可复现风险(R2运行时UNVERIFIED)。**注意**: 文件缺失≠历史结果虚假——历史指标已由现存产物精确复算证实(E019/E020)。
- 复现: 新开终端执行 `pwsh scripts/predict_best_fusion_crop.ps1 -Source <dir>` → 找不到 python.exe。
- 最小修复方向: ROOT/缓存路径参数化+相对默认值；提供锁定依赖清单与CPU/GPU双路径说明。
- 验收: 在未见过项目的干净环境中仅凭 README 指令完成一次小样推理并产出合法 JSON。

## F-11 · P1 · 内部验证集存在同源近邻帧泄漏，性能外推可信度受限 — CONFIRMED（阶段4升级为内容级硬证据；幅度待测试集检验）

- 受影响维度: 可信P/R性能与阈值策略(权重25)、鲁棒性泛化
- 事实(内容级, A级[E025]): 全量1100图 dHash64 比对——val↔train 最近距离为 **0 的有22张**(像素级近似复制)，≤8 共85张(38.6%)，中位最近距离12。命名启发式(E021的"98.2%≤10帧差")经目检修正: 命名族是批处理计数器而非视频帧号，序号相邻≠同源(dp_20329灶台 vs dp_20330仙女棒)，故该口径降级为粗分组参考，以内容哈希为准。
- 根因不变: `coco_to_yolo.py:105-118` seed42 单图随机划分、无任何分组 [E018]。
- 影响: 内部成绩(0.9474/.9939 等)是在"与训练集高度相似图像"上取得的，偏乐观且无法给出无偏泛化下界；哪怕按最宽的5族cluster bootstrap CI 也解释不了该量级的系统偏置[METRICS.md §3]。
- 处置(阶段4已交付提案, 未执行训练): machine/f11_proposed_split.json —— dHash≤8连通分量整簇重划(880/220→922/178, 挪动84图)，重划后 d≤8 重叠=0；**事后重划不能消除既有权重的训练污染**(用户指令明示)，重训成本方案见 F11_SPLIT_PROPOSAL.md §6(GPU≈5–6h级) 待另行审批。官方口径与严格泛化审计两条线分开报告。
- 复现: scripts/audit_phase45_f11_split.py → machine/f11_proposed_split.json / f11_moved_files.csv。

## F-12 · P2 · 官方口径度量实现的口径偏差与静默零处理 — CONFIRMED（阶段4完成量化：机制成立A，现存产物未受影响）

- 影响: R4/提交期操作风险
- 事实1: `evaluate_image_level.py:29-43` 以 gt∩pred **交集**计指标：缺失预测既不进 TP/FN 也不进分母(仅打印)；FP 口径中 extra keys 被忽略 → 若提交时漏图，P 与 R 均虚高且无告警退出。事实2: 分母为零时静默取 0.0。事实3: `build_cached_crop_rescue.py:63` 直接 `pred[name]`，缺键则崩溃(另一极端)。
- **量化(E026, A级)**: 模拟丢弃5张真阳性预测——交集口径 P=.9464/R=.9938；严格全覆盖口径(缺失计FN) R=.9636。缺5图即可使 R 虚高约 **+3.0个百分点** 且零告警；FP 偏差机制同源。
- 边界澄清(防止误读): 现存全部 val_pred_* JSON 均为220/220完整覆盖[E019]，故**历史已存储数字未受此影响、仍以 E019/E020 复算为准**；该机制属于"系统性抬高风险"，在官方测试集推理漏图时才会兑现为实际虚高。
- 修复方向: 统一"必须全覆盖"，缺失→非零退出码+名单输出；零分母策略文档化。
- **阶段7回归拦截(已验证, E032)**: 独立校验器自证套件把上述机制全部转为必失败用例——缺5真阳(T05)、缺负(T06)、空预测(T04)、重复键(T07)、NaN字面量(T11)、类型陷阱true/1.0/"1"(T08–T10)、大小写与缓存键冲突(T14/T16)、缺键直访崩溃等价物(T17)共21例全过；生产代码未动，此为提交侧独立防线。
- **R1整改(2026-08-27, 生产代码已修, G3)**: evaluate_image_level.py 重写为全集口径——GT为唯一分母宇宙；缺失/多余预测 fail-closed 退出码2并列名单；缺失在诊断列按其GT标签计FN(strict R=1/6 对缺5真阳反例, 不再出现 .9938/1.0 虚高)；零分母 None+undefined_flags 不再伪装0.0；合同解析拒绝重复键/NaN/true/1.0/"1"/越界。全覆盖时 stdout 与旧版逐字节一致(C05实测)，现存产物回归 == E019(.9474/.9818/.9643, C01实测)。状态改为 **FIXED(code, 单元级+评测器CLI级验证)**；端到端推理验证仍待 G5/G7。证据: remediation_r1/diffs/g3_*.patch, TEST_RESULTS.json(T-G3系列)。
- 验收: 两实现在缺失/多余键用例下行为一致且有显式提示。（生产侧仍未整改；见 P6/阶段8 后续建议）

## F-13 · P2 · 正负样本比例与未知测试先验带来的 Precision 敏感性未量化 — CONFIRMED(现状)/影响待核实

- 事实: 官方1100图正类864(78.5%)；val 220 中正165(75%)。提交链最终 P=0.948(9 FP)非常依赖测试集真实正类率(未知/待主办方确认)。若测试集负样本占比升高，同样误报行为将拉低 P。
- 建议: 报告时同时给出混淆矩阵与“若负占比翻倍时P的理论下界”式敏感性说明（分析性, 不改模型）。
- 验收: METRICS.md 含不同先验下的 P/R 情景表。

## F-03 · P1→按仓库口径升为提交材料缺口 — OPEN(部分新证)

- 云端部署/可视化实物(Dockerfile、app.py、models/)位于仓库外的桌面目录且本机缺失[E009,E001§8]，任务书硬性条目R5/R6在**可交付物层面**无证据(C级声明)。合并 F-05 残余(应用运行时一致性UNVERIFIED)。

## F-06 · P2 · 批量提交脚本与网页应用融合规则可能不一致 — OPEN(待核)

- 导航/README 自述应用含额外全图补救(SigLIP≥0.85 AND DINOv3≥0.70)，批量链不含[E001§6,E002]。该规则的离线效果已被复算为可行(164TP救回2图,E020)，但批量脚本未集成；两侧 parity 无法在本机实测(应用缺失)。
- 修复方向: 将全图补救并入 predict_best_fusion.py 输出后重跑验证链再冻结。

## F-02 · P2 · 大量绝对个人路径贯穿 scripts — CONFIRMED

- 例: predict_best_fusion_crop.ps1:4,11-21; resume/tune/tain ps1 中的 torch_gpu 绝对环境路径[E012/E015]。移植性差, 或暴露用户名信息(含中文人名目录)于文档。
- 附注(安全): 文档明文包含形如 `C:\Users\<姓名>\Desktop\...` 的个人信息, 公开仓库建议脱敏。

## F-08 · P2 · 标注争议样本机制未闭环 — OPEN(部分澄清)

- 澄清: COCO框标注与图像级标签全局一致(236零框图恰好=全部负类,无“有框标0”也无“标1无框”)[E019],故 docs 所述 20411 空标签争议属**检测框完备性**问题而非两类标签互斥冲突；同类完整清单未见。
- 建议: 阶段6对高置信FN统一抽样看图归类。

## F-09 · P2 · 外部数据许可证未记载 — PARTIAL-CONFIRMED

- build_manifest.json 记录了来源量与转换规则(E022, B级)；仓库与 external_datasets 内均无 LICENSE/README(find 无命中)。外部模型不在最终链但仍在产物中,答辩需交代。

## F-04 · Note · 多出的第1101文件为字节级重复副本 — RESOLVED

- `raw_fire_relabel_dp_20402(1).jpg`(231,172B) 与规范名文件 SHA-256 完全相同(followup json),Windows“(1)”复制残留；不入任何官方集合[set_coco 不含它]。建议清理磁盘即可,无评测影响。

## F-14 · P2 · 单图损坏将中止整批推理且不产出任何 JSON — CONFIRMED(静态) · 现存数据0损坏(阶段4实测)

- predict_best_fusion 各 predict_* 循环无 per-image 异常隔离；写盘在全部循环结束后[L589-624] → 中途异常=无输出文件。比赛时限内触发即高危。
- 阶段4补充: 对官方 raw 目录1101张图做 PIL verify+load 双重扫描 → **0 张损坏**[E027]。即当前该风险纯属" prospective(未来测试集可能遇到)"，与 F-12 同属提交期操作面风险而非现存产物缺陷。
- **阶段7补充(E032)**: 校验器新增 `preflight-images` 子命令作提交前筛查点——夹具注入截断JPEG+零字节文件被逐文件指名、rc=1(T20)；生产链未修改，"整批中止无JSON"缺陷本身仍开放。
- 修复方向: try/except + 显式错误占位策略(记录名单), 提交前自检键数==源图数(校验器已可直接承担该自检)。
- **R1整改(2026-08-27, 生产代码已修, G4)**: 新增纯标准库 batch_safety.py(record_failure/run_items_isolated/atomic_write/PARTIAL产物纪律/EXIT=3)；predict_best_fusion.py 五个打分函数全部逐图隔离(load_preprocess逐图跳坏图, inference失败逐图登记, 继续处理其余)，融合循环缺上游分数→记录并continue(不再KeyError整批中止)；有失败→主提交文件名不写, 改写 `<stem>_PARTIAL.json`+`_PARTIAL_details.csv`+`_failures.json` 原子落盘并退出码3；全正常→文件名/内容结构与旧行为一致(改原子写)。阈值/权重/imgsz等13项默认值由AST断言冻结(s01)防漂移。状态改为 **FIXED(code, 单元级+静态结构验证, 注入异常为mock)**；真实YOLO/VLM前向下的端到端行为仍待 G5/G7 复验。证据: remediation_r1/diffs/g4_*.patch, TEST_RESULTS.json(T-G4系列+h09校验器拒收partial)。

## F-15 · P2 · "应用全图补救"规则(0.85/0.70)系事后引入的同集选优 —— CONFIRMED(按仓库证据)/意图不可验证

- 影响: R9/E013 数字 0.9480/.9939 的定性标注
- 事实[A级证据链, E028]: 全库网格产物 `siglip_yolo_fusion_search_all.csv`(mtime 07-21 10:21)列结构仅 YOLO-vote×SigLIP 两维、**不存在 DINO 维度、亦无 (0.85,0.70) 行**；批量 live 产物 07-21 22:19；写有该规则的 README/导航 mtime 均晚于产物(07-22/08-05)。→ 仓库内找不到该规则**预先固定**(pre-registered)的痕迹。
- 结论: 按审计口径标注为"在同一验证集上事后引入的候选规则"，其 F1=.9704 属于乐观上界式的内部数字，不得表述为预先注册策略的验证结果；该规则亦未集成进批量提交脚本(F-06 关联)。
- 边界声明: "事后选点"在阈值搜索文化中常见且计算诚实(E020 复算吻合)，本条不是造假指控，而是**报告口径**问题; 无法验证作者当时的心理顺序(U)。

## Note 集合

- N1: COCO 有1个 bbox 越界(±2px容差)(1445中占比极小)[E019]; 类别单一 fire,1445框, 面积中位数3316px²(小目标为主)。
- N2: runs/detect/runs/detect 双嵌套 fire_yolo26m_aug_960 为游离重复训练产物, 未被融合引用(F-07 RESOLVED→Note)。
- N3: 阈值扫描 artifacts ×8+ 都在同一220验证集上选点(F-10 计入F-11综合评估); 阶段4扫描复核: 当前工作点 τs=0.22 恰为该集 argmax-F1(.9643)[E024] —— 与 N3/F-15 的"同集选优"定性互相印证。
- N4: requirements.txt 与实际训练环境(app 自带 requirements)脱节, 版本未锁(torch>=2.0.0 等)。
- N5: legacy `fire_yolov8n_960/args.yaml` 含第二个个人绝对路径(`C:\Users\<姓名>\Desktop\fire_detection\data\data.yaml`)——与部署目录路径(另一个人名)并存，公开仓库前应统一脱敏[F-02加重证据]。
- N6: 全库 JSON 扫描证实**不存在任何 ≥1100 键的预测文件**(E034)——本地既无官方测试集清单也无正式提交件；这是赛事阶段性事实而非缺陷，但意味着"阶段7结构校验通过"≠"提交就绪"(提交文件须待测试集后现场产出并校验)。
