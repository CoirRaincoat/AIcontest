# 无人值守流水线最终汇报（≤30 行）

1. **停止点/原因**: G1 正式训练前停止 —— 三模型重训投影 9.7h（m 3.83h + s 2.77h + s_aug 3.09h）超过冻结 8h GPU 预算（5060 比历史 5070 Ti 慢 1.7–2.4×），按「投影超则停止」条款未启动正式训练。
2. **环境是否冻结**: 是。`sf2026_min`（py3.11.9/torch2.8.0+cu128/ultralytics8.4.87/transformers5.16.1/timm1.0.28）验收门 17/17 PASS，frozen_requirements + wheel sha256 归档。
3. **G3/G4 是否端到端关闭**: 是。G3 220 图全链输出 == E019 逐字节一致；G4 坏图注入 rc3+PARTIAL+validator 拒收。F-12/F-14 由 FIXED(code) 升级 CLOSED。
4. **P6 结论**: 预测位 12/12 精确一致；分数 11/12 在 ±1e-3，1 图 dino 漂移 2e-3（FP16 跨 GPU 数值效应，不影响预测），已记录未调参。
5. **G7 延迟+显存**: 冷启动 59.08s、暖 12 图 28.22s（均摊 2.35s/图）、单图上界 19.71s；峰值显存 torch 1777MiB/nvidia-smi 5181MiB；峰值温度 59°C。
6. **G1 完成度/时长/最佳 ckpt**: 划分冻结完成（三集 698/198/146+隔离 58，跨集 d≤8=0）；正式训练 0 轮、无 ckpt（干跑 m/s 各 1 轮仅验证路径/OOM，指标未读）。
7. **holdout P/R+CI**: 未评估（G2 依赖 G1，被阻塞）。
8. **F-11 是否解除**: 未解除。划分已冻结但重训被预算门停止，权重仍为全量 1100 图训练产物，数据泄漏仍在。
9. **G6 完成度**: 未执行（依赖 G1 reg1 资产）；驱动已就绪，可授权用原始生产模型做 G1 无关的 surrogate 彩排。
10. **新评分+仍生效硬门槛**: 维持 NOT READY · 39/100（绑定「数据泄漏≤39」未解除）；「无端到端≤49」已解除；「无独立可信val≤69」仍生效；无官方测试集不解除提交门槛。
11. **新增目录/恢复入口/人工决策**: 新目录 remediation_r2b/r3/g1/g2/g6 + C:\fire_envs\；恢复入口 unattended_pipeline_status.json + G1_TRAINING_BLOCKED.md；需决策①放宽 8h 预算或换 5070 Ti 机②是否允许原始模型 G6 彩排。
