# REQUIREMENTS_MATRIX.md — 赛题 SF-2026-01 合规矩阵

性质列: 硬性=任务书明确要求；推荐=任务书推荐工具项；未知=任务书未规定。状态: PASS / PARTIAL / FAIL / UNVERIFIED。
更新: 2026-08-27 阶段1完成；阶段4–5 增注如下（状态未变，证据加密）：
- R2/PARTIAL 新增A证: E030/E031 权重↔配置↔代码一致性核对通过，但运行时缺口仍由 F-01 主导（P6_APPROVAL_REQUEST.md 已交）。
- R4/PARTIAL 新增量化: F-12 缺图虚高 +3.0pp R 的演示(E026) 与 0/1101 坏图扫描(E027) —— 校验器建议(阶段7)更显必要。
- R9/PARTIAL 新增 F-15: 文档成绩 0.9480/.9939 对应的"应用全图补救"无预先固定痕迹(E028)，答辩表述须按"同集事后候选规则"标注。
- 全局注记: 所有内部验证指标均须附"可能受近邻帧/序列泄漏影响，偏乐观"[F-11/E025]；阈值扫描不得用于提交择优[E024]。

## 阶段7 增注（2026-08-27）

- R4/PARTIAL 保持: 独立校验器(p7_validator.py)已交付并全量自证(E032)——重复键/NaN/类型/覆盖/大小写/路径穿越/F-12交集口径全部可拦截，且不依赖生产代码；但**生产链本身未整改**(仍无 per-image 隔离、评测口径未统一)，故状态不变。建议: 官方测试集到位后以"生产脚本产出 → 校验器无GT模式复核"为标准提交流程。
- R4 更新(R1整改, E035–E037): F-12/F-14 的生产侧修复已落地(G3全集口径评测器+G4逐图隔离与PARTIAL纪律, 30/30测试通过, 现存指标==E019不变)。状态仍记 **PARTIAL→"代码已修, 单元级验证"**: 真实推理链端到端行为(导入运行、真坏图注入、正式JSON)待 G5/G7/G6 门批准后复验, 届时方可升PASS。
- R10/PASS 补强: 校验器即此前矩阵建议的"独立严格校验器"，已落地：missing/extra/case-conflict fail-closed + 1101 文件显式排除记录[E033]。
- 结构校验能力通过 ≠ 端到端提交就绪[N6/E034]: 本地无官方测试集清单、无任何1100键JSON。

| # | 要求 | 来源/性质 | 项目证据 | 验证动作 | 状态 | 影响 | 建议 |
|---|---|---|---|---|---|---|---|
| R1 | 图像输入入口 | 任务书§4/§5 硬性 | `scripts/predict_best_fusion_crop.ps1` 强制参数 `$Source`，接收单图或目录(RGLOB, 去重名保护) [E014,E015] | 静态全读 | **PARTIAL** | 入口存在；默认所有路径指向不存在的 `C:\AI`，本机不可直接运行(F-01) | 参数化 ROOT |
| R2 | 模型加载与可运行推理 | §4 硬性 | `src/predict_best_fusion.py` 加载6自定义权重+本地骨干(local_files_only=True) [E014] | 代码级验证；运行时验证因缺 ultralytics/transformers/timm 且无 conda 环境而受阻 [E004,E009] | **PARTIAL**(代码A级, 运行时UNVERIFIED本机) | 无法在本机端到端复跑 → 复现性证据缺口 | 补一套锁定的 requirements + 相对路径默认值 |
| R3 | 图像级火/无火标签输出 | §8/§9 硬性 | `predictions[name]=int(base OR rescue)` 仅0/1 [E014 L594-603] | 静态+产物核验：5个预测JSON全部为int 0/1 [E019] | **PASS** | 正类语义=存在火焰(train_coco类别名"fire")；未见0/1反转 | 保持 |
| R4 | 严格JSON结果文件 | §9 硬性 | `json.dumps(dict[int], ensure_ascii=False)` 写UTF-8 [E014 L621-624] | 键值类型逐一校验通过 [E019] | **PARTIAL** | 格式合规律高；但批次中任一图像解码失败将中止全程、**不产出任何JSON**(写盘发生在末尾)[E014 无try/except]，且无完整性自检 | 增加 per-image 异常策略+输出前自检(F-14,F-12) |
| R5 | 可视化展示 | §5 硬性 | 导航文档描述 Gradio 应用(画框+分数) [E001§8] | 应用目录在仓库外且本机不存在 [E009] | **FAIL**(仓库内无实物) | 评审无法从提交物内看到可视化系统；目前仅 C 级声明 | 将 fire_demo 收进仓库或提供可访问地址 |
| R6 | 云端部署支持 | §4 硬性 | 文档提及 HF Spaces 配置/Dockerfile/DEPLOYMENT_GUIDE 位于仓库外应用目录 [E001§8,E002] | 同上，物理不存在可查 | **FAIL**(声明级) | “支持云端部署”当前无执行证据(仅文档一句) | 至少入库 Dockerfile+启动脚本+最小部署验证截图/URL |
| R7 | 实用性与可部署性 | §6 硬性 | 六模型融合+GPU推理+离线hf_cache；另有 npz 缓存离线复算线 [E014,E012] | 静态评估 | **PARTIAL** | 方案可用但资源需求重(3×YOLO26@960+SigLIP2+DINOv3)，量化/裁剪未见；时延未测(官方亦未设限,未知) | 记录单图时延基线即可形成证据 |
| R8 | 额外数据审查(来源/许可/污染) | §7 硬性 | `data_ext_v1/build_manifest.json`: DFire 添加800火+1200负样本,seed42,类别重映射规则明确 [E022]；最终三模型 args.yaml 均 `data/data.yaml`(纯官方) [E022] | 清单核对+训练配置核对 | **PARTIAL** | 最终融合链不含外部数据(降低风险)；但 DFire 许可证/引用方式未记载于仓库(U)；ext_v1模型存在若答辩展示需交代许可 | 补 LICENSE/引用页；明确“外部数据仅用于对照实验”的书面说明 |
| R9 | 提交/环境/模型说明与答辩材料 | 初赛评审 推导(未知具体格式) | 导航文档含完整提交流程和权重说明 [E001§7,E002] | 与实际核对：环境锚点失效 [E009] | **PARTIAL** | 流程清楚但复现步骤绑定已消失的本机绝对路径/conda环境 | 重写 README 快速开始段为可移植版本 |
| R10 | 测试集全覆盖(逐图输出一次) | §9 硬性推导 | `iter_images`: rglob排序+重复名抛错+空目录报错 [E014 L111-124]；已算链覆盖220/220无缺 [E019] | 代码审读+产物核算 | **PASS**(机制层) | 大小写碰撞只在同目录rglob内防了不同目录场景(已有防重复名检查)；建议提交前用独立校验器复核 | 保留并落实阶段7的严格校验器 |

## 不得补造事项声明

官方未公布: P/R合成公式、权重阈值、硬件/时延/模型大小上限、是否烟雾计正类、测试集规模。本矩阵一律未填写对应“达标结论”，统一标注为**未知/待主办方确认**。
