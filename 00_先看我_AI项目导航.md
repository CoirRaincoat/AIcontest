# C:\AI 火情识别项目导航

更新时间：2026-07-22  
项目编号：SF-2026-01  
主项目：`C:\AI\fire_detection`

## 1. 先记住这四件事

1. 比赛最终评估是**图像级二分类**：有火为 `1`，无火为 `0`。
2. `best.pt` 是模型权重，不是直接上传的比赛答案。
3. 测试集发布后，需要用模型和推理代码生成以图片名为键的 JSON 文件。
4. 当前最佳方案不是一个单独模型，而是 3 个 YOLO + SigLIP2 + DINOv3 + 局部 SigLIP2 的融合。

当前数据划分：

- 训练集：880 张图片，880 个 YOLO 标签文件。
- 验证集：220 张图片，220 个 YOLO 标签文件。
- 官方原始训练资源：约 1100 张图片、COCO 目标框标签和图像级标签。
- 正式测试集：尚未发布。

## 2. 最常用的路径

| 要找的内容 | 路径 |
|---|---|
| 项目根目录 | `C:\AI\fire_detection` |
| 官方原始图片 | `C:\AI\fire_detection\data\images\train\images` |
| 官方原始 COCO 框标签 | `C:\AI\fire_detection\data\images\train\train_coco.json` |
| 官方原始图像级标签 | `C:\AI\fire_detection\data\images\train\train_image.json` |
| YOLO 训练图片 | `C:\AI\fire_detection\data\train\images` |
| YOLO 训练标签 | `C:\AI\fire_detection\data\train\labels` |
| YOLO 验证图片 | `C:\AI\fire_detection\data\val\images` |
| YOLO 验证标签 | `C:\AI\fire_detection\data\val\labels` |
| 数据配置 | `C:\AI\fire_detection\data\data.yaml` |
| 最终 YOLO 权重 | `C:\AI\fire_detection\runs\detect\runs\train` |
| SigLIP2 分类头 | `C:\AI\fire_detection\runs_siglip` |
| DINOv3 分类头 | `C:\AI\fire_detection\runs_dinov3` |
| 验证预测和指标 | `C:\AI\fire_detection\outputs` |
| Python 源码 | `C:\AI\fire_detection\src` |
| PowerShell 操作脚本 | `C:\AI\fire_detection\scripts` |
| 本地/云端可视化应用 | `C:\Users\薇尔莉特\Desktop\fire_detection\deployment\fire_demo` |

## 3. 数据和标签怎么理解

### 3.1 官方原始标签

`train_coco.json` 是目标检测标签，记录火焰框的位置。它适合训练 YOLO。

`train_image.json` 是图像级标签，只回答整张图片有没有火。它与比赛测试答案的格式最接近。

不要把这两个 JSON 当成重复文件：一个用于定位火焰，一个用于整图判断。

### 3.2 转换后的 YOLO 标签

`data\train\labels` 和 `data\val\labels` 中，每张图片对应一个同名 `.txt`。

每行格式：

```text
class_id center_x center_y width height
```

- 当前只有一类，`class_id=0` 表示火焰。
- 后四个数字是相对图片宽高归一化后的坐标，范围通常是 0 到 1。
- 空标签文件表示这张图片没有目标框。
- 图片与标签必须同名，例如 `abc.jpg` 对应 `abc.txt`。

注意：空标签不一定永远符合肉眼判断。已经发现 `raw_fire_relabel_dp_20411.txt` 是空标签，但画面中存在疑似火焰区域。这属于需要复核的标签争议，不能只靠降低阈值解决。

### 3.3 外部数据

- DFire 原始数据：`C:\AI\external_datasets\DFire`
- 官方数据与部分外部数据融合后的版本：`C:\AI\fire_detection\data_ext_v1`
- 局部候选框训练数据：`C:\AI\fire_detection\crop_data_v2`
- SigLIP2 图像级清单：`C:\AI\fire_detection\siglip_data`
- DINOv3 图像级清单：`C:\AI\fire_detection\dinov3_data`

`crop_data_v2` 不是官方原始数据。它由真实火焰框、YOLO 候选框、困难负样本和无火图片切块生成，用于训练局部 SigLIP2 补救模型。

## 4. 模型权重在哪里

### 4.1 最终采用的 YOLO 权重

```text
C:\AI\fire_detection\runs\detect\runs\train\fire_yolo26m_960\weights\best.pt
C:\AI\fire_detection\runs\detect\runs\train\fire_yolo26s_960\weights\best.pt
C:\AI\fire_detection\runs\detect\runs\train\fire_yolo26s_aug_960\weights\best.pt
```

三个权重分别负责模型规模差异和增强训练差异。最终融合规则只要其中至少一个 YOLO 通过，就认为存在 YOLO 火焰证据。

### 4.2 三个分类头

```text
全图 SigLIP2：
C:\AI\fire_detection\runs_siglip\siglip2_linear_seed2026\best_head.pt

DINOv3：
C:\AI\fire_detection\runs_dinov3\dinov3_vitb16_linear_seed42\best_head.pt

局部 SigLIP2：
C:\AI\fire_detection\runs_siglip\siglip2_crop_v2_seed2026\best_head.pt
```

这些 `best_head.pt` 只有约 15 KB，因为它们只保存最后的线性分类头。完整 SigLIP2 和 DINOv3 骨干模型位于：

```text
C:\AI\hf_cache
```

因此不要删除 `hf_cache`，否则离线推理无法加载两个骨干模型。

### 4.3 预训练权重与训练后权重的区别

项目根目录下的 `yolo26n.pt`、`yolo26s.pt`、`yolo26m.pt` 是开始训练时使用的预训练权重。

训练目录里的 `weights\best.pt` 才是经过火灾数据训练后、验证表现最好的权重。

`last.pt` 是最后一轮权重，主要用于断点续训，不代表效果最好。推理和提交优先使用 `best.pt`。

## 5. 去哪里看训练过程和效果

打开某个 YOLO 训练目录，例如：

```text
C:\AI\fire_detection\runs\detect\runs\train\fire_yolo26m_960
```

重要文件：

- `args.yaml`：实际训练参数，包括 epochs、batch、imgsz 和数据路径。
- `results.csv`：每轮损失、Precision、Recall 和 mAP。
- `results.png`：训练曲线图。
- `confusion_matrix.png`：混淆矩阵。
- `BoxPR_curve.png`、`BoxF1_curve.png`：检测阈值相关曲线。
- `weights\best.pt`：最佳权重。
- `weights\last.pt`：最后一轮权重。

比赛使用图像级 Precision 和 Recall，因此 YOLO 的框级 mAP 不能直接等同于比赛成绩。最终图像级结果在 `outputs` 中查看。

当前重要验证文件：

```text
C:\AI\fire_detection\outputs\val_gt.json
C:\AI\fire_detection\outputs\val_pred_best_siglip_yolo_dino_crop_live_details.csv
C:\AI\fire_detection\outputs\val_pred_best_siglip_yolo_dino_crop_rescue_thr097_metrics.json
```

## 6. 当前融合方案

原稳健批量方案：

```text
base =
(YOLO26m OR YOLO26s OR YOLO26s_aug)
AND 全图 SigLIP2
AND DINOv3

final = base OR 局部 SigLIP2 高置信补救
```

验证结果：Precision 0.9474，Recall 0.9818，F1 0.9643。

网页应用后来增加了全图双模型补救：

```text
全图 SigLIP2 >= 0.85 AND DINOv3 >= 0.70
```

加入后验证结果：Precision 0.9480，Recall 0.9939，F1 0.9704。

重要区别：当前网页应用已经包含新补救规则，但 `scripts\predict_best_fusion_crop.ps1` 对应的批量推理源码仍是原稳健批量方案。正式测试集发布前，应先把新规则同步到批量脚本并再次完整验证，不能默认两者输出完全相同。

## 7. 比赛究竟要上交什么

比赛细则明确要求：

1. 完成模型和推理代码。
2. 支持云端部署。
3. 实现从图片输入、算法识别、标签输出到可视化展示的完整功能。
4. 测试集结果保存为 JSON，以图片名为键，以 `0/1` 为值。

JSON 示例：

```json
{
  "xxx1.jpg": 0,
  "xxx2.jpg": 1
}
```

因此需要准备三类成果：

- **比赛答案**：正式测试集对应的 JSON 文件。这通常是评测分数的直接输入。
- **模型与推理代码**：六个自定义权重、推理源码、依赖说明和启动脚本。
- **云端与可视化系统**：Gradio 应用及其公开或评审可访问地址。

平台最终有哪些上传框、是否还要求说明文档或压缩包，以焕新社区截止前的页面通知为准。细则没有明确要求把某个单独 `best.pt` 当作答案上传。

### 测试集发布后的标准流程

1. 将测试图片放入一个单独的英文路径，例如 `C:\AI\fire_detection\test_images`。
2. 检查图片数量和文件名，避免重复文件名或混入非图片文件。
3. 使用最终批量融合脚本生成 JSON。
4. 检查 JSON 的键数量等于测试图片数量。
5. 检查每个值只能是整数 `0` 或 `1`。
6. 随机抽查若干有火和无火结果。
7. 将最终 JSON 上传到平台，不要把验证集 JSON 误当作测试集答案。
8. 按平台要求同时提交模型、推理代码和云端展示地址。

当前批量入口：

```text
C:\AI\fire_detection\scripts\predict_best_fusion_crop.ps1
```

当前核心推理源码：

```text
C:\AI\fire_detection\src\predict_best_fusion.py
```

## 8. 可视化应用在哪里

应用目录：

```text
C:\Users\薇尔莉特\Desktop\fire_detection\deployment\fire_demo
```

主要文件：

- `app.py`：上传图片、融合推理、画框和模型分数展示。
- `start_local.ps1`：本机启动。
- `models`：云端部署所需的六个自定义权重副本。
- `requirements.txt`：Python 依赖。
- `README.md`：Hugging Face Spaces 配置。
- `Dockerfile`：Docker 部署配置。
- `DEPLOYMENT_GUIDE.md`：云端部署步骤。

本机地址通常是：

```text
http://127.0.0.1:7860
```

云端不能读取本机的 `C:\AI`，所以云端项目必须携带 `fire_demo\models` 中的六个权重，并在线下载或缓存 SigLIP2、DINOv3 骨干模型。

## 9. 哪些目录不要随便删除

- `data`：官方原始数据、转换后数据和标签。
- `runs\detect\runs\train\fire_yolo26m_960`：最终 YOLO26m。
- `runs\detect\runs\train\fire_yolo26s_960`：最终 YOLO26s。
- `runs\detect\runs\train\fire_yolo26s_aug_960`：最终增强 YOLO26s。
- `runs_siglip`：SigLIP2 分类头和训练记录。
- `runs_dinov3`：DINOv3 分类头和训练记录。
- `hf_cache`：SigLIP2 和 DINOv3 完整骨干权重。
- `outputs`：验证真值、阈值结果和最终融合指标。
- `src`、`scripts`：训练、评估和提交代码。
- `crop_data_v2`：局部模型训练数据。
- `C:\Users\薇尔莉特\Desktop\fire_detection\deployment\fire_demo`：可视化和云端部署应用。

## 10. 2026-07-22 清理记录

经确认并在备份存在的前提下，已删除：

- `C:\AI\fire_detection\models`：PyTorch 安装包副本。
- `C:\AI\fire_detection\crop_data_v1`：已被 v2 替代的局部数据。
- `fire_yolov8s_960`：未采用的旧 YOLOv8s 实验。
- `fire_yolov8s_960_pause_backup`：暂停训练备份。
- `gpu_smoke_test`：GPU 冒烟测试。
- `C:\AI\fire_detection\runs\runs\train`：早期 CPU、v1、v2 试跑。

实际释放约 1921.9 MB。其余候选内容未删除。

## 11. 接下来最重要的三步

1. 复核争议标签，尤其是肉眼有火但标签为空的图片，不要直接批量改标签。
2. 将网页应用新增的“全图双模型补救”同步到正式批量提交脚本，并重新生成完整验证指标。
3. 测试集发布后只做推理和结果检查，不再临时大规模改变模型结构或训练数据。
